on:
  push:
  workflow_dispatch:
jobs:
  print:
    name: Print
    runs-on: ubuntu-latest
    steps:
      - run: pwd
      - run: ls -l -a
      - run: ls -l -a ${{ github.workspace	}}
      - uses: actions/checkout@v2
      - run: ls -l -a
      - uses: ./.github/actions/test